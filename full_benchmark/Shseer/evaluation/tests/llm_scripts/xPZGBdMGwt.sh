
#!/bin/sh

# Specify the directory for which we want to display the disk usage
directory="/path/to/directory"

# Use the du command to display the disk usage of the specified directory
du -h $directory
