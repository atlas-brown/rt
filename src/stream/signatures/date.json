{
    "command_name": "date",
    "default_input_type": ".*",
    "default_output_type": ".*",
    "ignore_input": true,
    "args": [
        {
            "name": "format",
            "is_regex": false
        }
    ],
    "flags": [],
    "rules": [
        {
            "condition": {
                "args": ["format"]
            },
            "update": {
                "output_type": "[0-9]+"
            }
        }
    ]
}