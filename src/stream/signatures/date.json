{
    "command_name": "date",
    "default_input_type": ".*",
    "default_output_type": ".*",
    "args": [
        {
            "name": "format",
            "is_regex": false
        }
    ],
    "flags": [],
    "rules": [
        {
            "condition": {
                "heuristics": ["no_ignore_input"]
            },
            "update": {
                "input_type": ""
            }
        },
        {
            "condition": {
                "args": ["format"]
            },
            "update": {
                "output_type": "[0-9]+"
            }
        }
    ]
}