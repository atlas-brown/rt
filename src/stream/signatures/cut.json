{
    "command_name": "cut",
    "default_input_type": ".*",
    "default_output_type": ".*",
    "args": [],
    "flags": [],
    "rules": [
        {
            "condition": {
            },
            "update": {
                "output_type": "{{actual_input_type}}"
            }
        }
    ]
}