command_name: cksum
default_input_type: .*
default_output_type: .*
args: []
flags: []
rules:
- condition:
    no_flags:
    - -a
    - --algorithm
    - -c
    - --check
  update:
    output_type: '[0-9]+ [0-9]+( .*)?'
isInteresting: true
