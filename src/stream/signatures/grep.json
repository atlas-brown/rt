{
    "command_name": "grep",
    "format": ["grep", "flag?", "arg"],
    "default_input_type": ".*",
    "default_output_type": ".*",
    "args": [
        {
            "name": "pattern",
            "type": ".+"
        }
    ],
    "flags": [
        {
            "name": "-i",
            "has_arg": false
        }
    ],
    "rules": [
        {
            "condition": {
                "flags": []
            },
            "output_type": ".*{pattern}.*&{actual_input_type}"
        }
    ]
}
