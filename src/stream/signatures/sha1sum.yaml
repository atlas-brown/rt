command_name: sha1sum
default_input_type: .*
default_output_type: .*
args: []
flags: []
rules:
- condition:
    no_flags:
    - -c
    - --check
    - --status
    - --quiet
    - --strict
    - --tag
  update:
    output_type: '[[:xdigit:]]{40} [ *](-|.+)'
isInteresting: true
