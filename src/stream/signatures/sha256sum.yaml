command_name: sha256sum
default_input_type: .*
default_output_type: .*
args: []
flags: []
rules:
- condition:
    no_flags:
    - -c
    - --check
    - --status
    - --quiet
    - --strict
    - --tag
  update:
    output_type: '[[:xdigit:]]{64} [ *](-|.+)'
isInteresting: true
