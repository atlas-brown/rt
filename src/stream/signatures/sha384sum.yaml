command_name: sha384sum
default_input_type: .*
default_output_type: .*
args: []
flags: []
rules:
- condition:
    no_flags:
    - -c
    - --check
    - --status
    - --quiet
    - --strict
    - --tag
  update:
    output_type: '[[:xdigit:]]{96} [ *](-|.+)'
isInteresting: true
