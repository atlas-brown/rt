{
    "command_name": "ls",
    "default_input_type": ".*",
    "default_output_type": ".*",
    "args": [],
    "flags": [],
    "rules": [
        {
            "condition": {
                "heuristics": ["no_ignore_input"]
            },
            "update": {
                "input_type": ""
            }
        },
        {
            "condition": {
                "flags": ["-l"]
            },
            "update": {
                "output_type": "(total [0-9]+)|([^ ]+ +[0-9]+ +[^ ]+ +[^ ]+ +[0-9]+ +[^ ]+ +[0-9]+ +[^ ]+ +.+)"
            }
        }
    ]
}