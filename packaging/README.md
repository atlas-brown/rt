# Packaging

Builds `.deb` (Ubuntu), `.rpm` (Fedora), and a Homebrew formula (MacOS) that install `rt` and `rtr` system-wide, with no build tools required
on the target machine. Only the checker CLIs are packaged, not the full benchmark reproduction pipeline.

